# Lines

There are some settings in `LineContext` that allow to configure rendering of line (`draw::line::between_two_points()` and other `draw::line::*`). As of April 2025, rendering of the lines is in an MVP state, so it may not be perfect. Hopefully, it will be improved in the foreseeable future.